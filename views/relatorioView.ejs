<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatorio de dados</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="http://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
</head>

<body>
    <div class="container mt-4">
        <h1 class="text-center mb-4">Relatorio de clientes</h1>
        <a href="relatorio/pdf">Gerar PDF</a>
        <table class="table table-bordered table-striped"></table>
        <thead class="thead-dark">
            <tr>
                <th>ID</th>
                <th>NOME</th>
                <th>E-MAIL</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(dado=> { %>
                <tr>
                    <td>
                        <%= dado.id %>
                    </td>
                    <td>
                        <%= dado.name %>
                    </td>
                    <td>
                        <%= dado.email %>
                    </td>
                </tr>
                <% }); %>
        </tbody>
        </table>
        <div class="text-right mt-4">
            <p class="text-muted">Página 1 de 1</p>
            <p class="text-muted">Gerado em: <%= new Date().toLocaleDateString() %>
            </p>
        </div>
    </div>
</body>

</html>